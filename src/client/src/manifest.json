{
    "manifest_version": 2,
    "name": "{{ name }}",
    "description": "{{ description }}",
    "version": "{{ version }}",
    "icons": {
        "128": "images/logo_128.png"
    },
    "app": {
        "urls": [
            "{{ &url }}"
        ],
        "launch": {
            "web_url": "{{ &url }}"
        }
    }
}
